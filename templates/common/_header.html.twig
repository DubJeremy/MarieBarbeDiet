<header>
    <nav>
        <div>
            <img src="{{asset('images/MarieBarbé.png')}}" alt="Logo Marie Barbé">
            <div>
                <h1> 
                    Diététicienne,
                    Nutritionniste.
                </h1>
            </div>
        </div>
        <div>
            <ul>
                <li>
                    <a  class='underline' href="{{path("app_home_index")}}">Accueil</a>
                </li>
                <li>
                    <a class='underline'href="{{path("app_contact_contact")}}">Contact</a>
                </li>
                {% if not app.user %}
                <li class="log">
                    <a class='underline' href="{{path("app_registration_register")}}">Inscription</a> / <a  class='underline' href="{{path("app_security_login")}}">Connexion</a>
                </li>
                {% else %}            
                    <li>
                        <a  class='underline' href="{{path("app_booking_index")}}">Rdv</a>
                    </li>
                    <li>
                        <a href="{{path("app_user_profile")}}"><img src="{{asset('images/media/' ~ app.user.profilePicture)}}" alt="Photo de profil"></a>
                    </li>
                    <li>
                        <a  class='underline' href="{{path("app_logout")}}">Déconnexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
        <div id="cross">
            <i class="fas fa-times"></i>
        </div>
    </nav>
    <div id="burger">
        <i class="fas fa-bars"></i>
    </div>
</header>
