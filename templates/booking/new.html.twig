{% extends 'base.html.twig' %}

{% block title %}Marie Barbe Avis{% endblock %}

{% block body %}
    {{form_start(TimeSlotForm)}}
        {{ form_row(TimeSlotForm.title, {
            label: 'Titre'
        }) }}
        {{ form_row(TimeSlotForm.start, {
            label: 'Début'
        }) }}
        {{ form_row(TimeSlotForm.end, {
            label: 'Fin'
        }) }} 
        {{ form_row(TimeSlotForm.description, {
            label: 'Déscription'
        }) }}
        {{ form_row(TimeSlotForm.backgroundColor, {
            label: 'Couleur'
        }) }}
        {{ form_row(TimeSlotForm.borderColor, {
            label: 'Couleur bordure'
        }) }}
        {{ form_row(TimeSlotForm.textColor, {
            label: 'Couleur du texte'
        }) }}
    {{form_end(TimeSlotForm)}}
{% endblock %}
